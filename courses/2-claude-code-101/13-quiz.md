# 13. Course Quiz

**Course:** [Claude Code 101](./index.md)

## Quiz Topics

**What is Claude Code?**
- The agentic loop, permissions, capabilities

**Your first prompt**
- Approval vs auto-accept modes (`Shift + Tab`)
- Plan mode

**Daily workflows**
- Explore → Plan → Code → Commit
- Context management (`/compact`, `/clear`)
- Code review with subagents, `--from-pr`

**Customizing Claude Code**
- CLAUDE.md — purpose, hierarchy, `/init`
- Subagents — isolated context, `/agents`
- Skills — `.claude/skills/`, `/skill-name`
- MCP — external services, scoping, context costs
- Hooks — deterministic, outside the loop

**Key commands**
`/compact`, `/clear`, `/init`, `/agents`, `/mcp`, `/commit-push-pr`
